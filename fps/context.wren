
class World {
  construct new() {}
  player { _player }
  player=(v) { _player = v }

  doors { _doors }
  doors=(v) { _doors = v }

  entities { _entities }
  entities=(v) { _entities = v }

  map { _map }
  map=(v) { _map = v }
}
